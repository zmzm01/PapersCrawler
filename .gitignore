data
slides
__pycache__
.coverage
coverage.xml
htmlcov/
.env
*.swp
graphify-out

# 运行时 SQLite 数据库（schema 在 src/db/database.py，data/papers.db 已在 data/ 下被忽略；
# 根目录 *.db 多为脚本意外产物，统一忽略）
*.db
*.sqlite
*.sqlite3

# 配置文件（实际配置从 git 排除，.example 模板保留）
configs/publishers.yaml
configs/keywords.yaml
configs/settings.yaml
configs/prompts/fix.yaml
configs/prompts/relevance.yaml
configs/prompts/summary.yaml

# 可部署的 cron 包装脚本允许纳入版本控制；机器专属脚本仍保持忽略。
run_hugo_deploy.sh

# SMTP 配置文件（已合并到 .env）
configs/email.yaml

# 评审记录 & AI 辅助文件（本地开发用，不提交）
docs/reviews/
.slim/
AGENTS.md
site/
site/public/

# Independent Astro report site: source is tracked, generated data/builds are not.
report-site/node_modules/
report-site/dist/
report-site/.astro/
report-site/src/data/reports/
report-site/src/generated-reports.ts
