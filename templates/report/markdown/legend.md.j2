{# ==========================================================================
   templates/report/markdown/legend.md.j2
   --------------------------------------------------------------------------
   报告头部的「相关性等级说明」图例（Markdown 引用块形式）。

   编辑此文件即可自定义图例文本。说明性术语保持与
   configs/prompts/relevance.yaml 中 Phase E LLM Prompt 的定义一致。
   ========================================================================== #}
{% macro legend(presentation=None) -%}
{% set presentation = presentation or build_report_presentation() -%}
> **相关性等级说明**
>
{% for item in presentation.relevanceLegend %}> - **{{ item.code }}**（{{ item.name }}）：{{ item.description }}
{% endfor -%}
{%- endmacro %}
