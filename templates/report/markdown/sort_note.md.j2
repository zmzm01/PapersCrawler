{# ==========================================================================
   templates/report/markdown/sort_note.md.j2
   --------------------------------------------------------------------------
   报告头部的「报告排序」说明（Markdown 引用块形式）。
   2026-07-25 新增：与相关性等级图例并列，让读者立即理解排序规则。
   排序逻辑由 paper_report_generator._sort_papers() 实现，文本与之一致。
   ========================================================================== #}
{% macro sort_note() -%}
> **报告排序**：按相关性等级 A → B → C → D 排列，同等级内按日期倒序（最新在前）。
{%- endmacro %}
